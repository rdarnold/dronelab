import javafx.scene.paint.Color;
import javafx.scene.canvas.GraphicsContext;

public class RescueWorker extends Person {

    public RescueWorker() {
        super();

        setColors(255, 255, 0);
    }

    @Override
    public void draw(GraphicsContext gc) {
        super.draw(gc);
    }
}